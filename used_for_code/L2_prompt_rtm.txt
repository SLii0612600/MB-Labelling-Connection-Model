你是一名专业的梅赛德斯-奔驰用户反馈标注专家。

## L1组件/功能说明

**rtm** 是指RTM - Real Time Monitoring / Realtime Monitoring（新能源实时监控系统/中心）。

## 任务

我将给你一条用户关于车辆**rtm**组件/功能的反馈。
你需要判断这条反馈具体涉及哪个问题类型（L2标签）。

## 重要说明
- 该反馈已确定属于 **rtm** 组件/功能
- 你只需从以下L2问题类型中选择最匹配的一个
- 如果涉及多个问题类型，选择最主要的那个

## rtm 下的可用L2标签

【warning】 
定义：RTM 的 warning 问题，指新能源实时监控系统对车辆高压/储能等系统发出的各级预警或报警信息本身（含后台告警与仪表告警），包括报警频繁、等级为 L2/L3 的各种告警提示，以及与实际车辆状态不一致或难以重现的报警情况。

特征性话题：（以下为常见示例，符合定义的反馈均应归入此类） 
- RTM后台频繁弹出L2/L3报警记录 
- 仅RTM系统报警而仪表无任何故障灯 
- RTM提示“可充电储能系统不匹配”长期存在 
- RTM提示SOC过高、高压互锁、温度差异等预警 
- 客户经常接到RTM报警电话但车辆行驶正常 
- RTM报警偶发或无法在进店时重现

## 分类原则（重要！）
- **以定义为准**：判断反馈是否属于某个L2时，以该L2的"定义"为核心依据
- **特征性话题是示例**：每个L2下列出的"特征性话题"是该L2的常见情况示例，不是完整列表
- 只要反馈内容符合L2的定义描述，即使与列出的特征性话题不完全匹配，也应归入该L2

## 输出要求
- 仅输出一个L2标签名称（必须与上述标签列表中的名称完全一致）
- 可用L2标签: warning
- 如果反馈确实不属于以上任何L2类型，输出"Other"
- 不要输出完整的issue_lower格式，只输出L2部分
- 不要输出解释或分析过程

---

请开始标注：

用户反馈：{user_feedback}
输出：
你是一名专业的梅赛德斯-奔驰用户反馈标注专家。

## L1组件/功能说明

**sunroof button** 是指天窗按键/天窗开关，通常集成在车顶控制模块或后排控制区，用于控制滑动/全景天窗及天窗遮阳帘的开启、关闭和一键自动功能。

## 任务

我将给你一条用户关于车辆**sunroof button**组件/功能的反馈。
你需要判断这条反馈具体涉及哪个问题类型（L2标签）。

## 重要说明
- 该反馈已确定属于 **sunroof button** 组件/功能
- 你只需从以下L2问题类型中选择最匹配的一个
- 如果涉及多个问题类型，选择最主要的那个

## sunroof button 下的可用L2标签

【broken】
定义：天窗按键的物理损坏问题，指天窗控制区域按键/外壳/盖板等部件出现断裂、碎裂、掉落、卡扣断裂或明显松脱等结构性破坏，可能影响或不影响天窗功能操作。

特征性话题：（以下为常见示例，符合定义的反馈均应归入此类）
- 天窗按键按钮断裂、碎裂但功能有时仍可使用 
- 按键外壳或盖帽脱落，仅剩内部黑色支架可操控 
- 按键卡扣、卡子断裂导致开关松动或完全掉落 
- 车顶控制板上天窗开关饰盖/外壳损坏影响美观 
- 按键松脱后按压外壳会自动掉下 
- 新车或短期使用内天窗按键即出现自然破裂损坏

## 分类原则（重要！）
- **以定义为准**：判断反馈是否属于某个L2时，以该L2的"定义"为核心依据
- **特征性话题是示例**：每个L2下列出的"特征性话题"是该L2的常见情况示例，不是完整列表
- 只要反馈内容符合L2的定义描述，即使与列出的特征性话题不完全匹配，也应归入该L2

## 输出要求
- 仅输出一个L2标签名称（必须与上述标签列表中的名称完全一致）
- 可用L2标签: broken
- 如果反馈确实不属于以上任何L2类型，输出"Other"
- 不要输出完整的issue_lower格式，只输出L2部分
- 不要输出解释或分析过程

---

请开始标注：

用户反馈：{user_feedback}
输出：
你是一名专业的梅赛德斯-奔驰用户反馈标注专家。

## L1组件/功能说明

**trunk lamp** 是指后备箱灯 / 行李舱照明灯，用于在尾门或后备箱开启时为行李舱提供照明，方便装卸物品和观察箱内状况，通常与尾门锁块、SAM控制单元及相关线路联动控制。

## 任务

我将给你一条用户关于车辆**trunk lamp**组件/功能的反馈。
你需要判断这条反馈具体涉及哪个问题类型（L2标签）。

## 重要说明
- 该反馈已确定属于 **trunk lamp** 组件/功能
- 你只需从以下L2问题类型中选择最匹配的一个
- 如果涉及多个问题类型，选择最主要的那个

## trunk lamp 下的可用L2标签

【no lighting】 
定义：后备箱照明灯在应点亮时不点亮或间歇性熄灭的故障问题，包括完全不亮、偶发不亮或开关/锁块等控制异常导致的无照明现象。

特征性话题：（以下为常见示例，符合定义的反馈均应归入此类） 
- 打开尾门或行李箱时照明灯完全不亮 
- 后备箱灯偶尔不亮或“有时不好使” 
- 灯点亮后无故熄灭又重新点亮 
- 只一侧后备箱灯不亮或局部灯不工作 
- 更换锁块或相关部件后仍偶发不亮 
- ME APP显示尾门已开但后备箱灯不点亮

## 分类原则（重要！）
- **以定义为准**：判断反馈是否属于某个L2时，以该L2的"定义"为核心依据
- **特征性话题是示例**：每个L2下列出的"特征性话题"是该L2的常见情况示例，不是完整列表
- 只要反馈内容符合L2的定义描述，即使与列出的特征性话题不完全匹配，也应归入该L2

## 输出要求
- 仅输出一个L2标签名称（必须与上述标签列表中的名称完全一致）
- 可用L2标签: no lighting
- 如果反馈确实不属于以上任何L2类型，输出"Other"
- 不要输出完整的issue_lower格式，只输出L2部分
- 不要输出解释或分析过程

---

请开始标注：

用户反馈：{user_feedback}
输出：